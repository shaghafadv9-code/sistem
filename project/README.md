# Smart Secretary — نظام السكرتير الذكي v1.0.0

نظام إدارة سكرتارية وأعمال احترافي لسطح المكتب (Windows) — عربي RTL بالكامل.

## الوحدات

لوحة قيادة ذكية • بحث موحد (Ctrl+K) • مهام (قائمة/كانبان/تقويم) • مواعيد (يوم/أسبوع/شهر/أجندة) •
عملاء CRM مع Timeline • اتصالات • ملاحظات • ملفات • إشعارات • مستخدمون وأدوار • سجل تدقيق •
مشاريع 101-108 ووحدات • حجوزات • مبيعات ومالية • 10 تقارير (PDF/Excel/طباعة) • مساعد ذكي عربي • نسخ احتياطي.

## التشغيل بضغطة واحدة

| الملف | النظام | الوظيفة |
|---|---|---|
| `run.bat` | Windows | تثبيت (أول مرة) + بناء + فتح التطبيق كنافذة سطح مكتب — **اضغط ضغطتين** |
| `run-dev.bat` | Windows | وضع التطوير: الخوادم + نافذة التطبيق |
| `run.sh` | Linux / macOS | نفس `run.bat` — `./run.sh` |
| `run-dev.sh` | Linux / macOS | وضع التطوير — `./run-dev.sh` |

المتطلب الوحيد: [Node.js LTS](https://nodejs.org).

## التشغيل (تطوير)

```bash
npm install
npm run dev        # API :3847 + Web :5173
```

ثم افتح `http://localhost:5173`

> لتجربة غلاف سطح المكتب أثناء التطوير: شغّل `npm run dev` ثم في نافذة أخرى `npx electron .`

## حسابات الدخول

| المستخدم | الدور | كلمة المرور |
|---|---|---|
| `admin` | مدير النظام | `admin123` |
| `sara` | سكرتيرة | `sara1234` |
| `accountant` | المحاسب | `acc12345` |
| `reservations` | مسؤول الحجوزات | `res12345` |

> غيّر كلمات المرور فور التسليم من صفحة المستخدمين.

## الاختبارات

```bash
npm test           # 46 فحصًا: دخول، صلاحيات، CRUD، مالية، حجوزات، تقارير، مساعد، نسخ
```

## بناء نسخة الإنتاج (على Windows)

المتطلبات: Node.js 20+ فقط — لا حاجة لأي أدوات بناء (قاعدة البيانات WASM خالصة بدون ترجمة).

```bash
npm install
npm run dist       # ينتج Smart-Secretary-Setup-1.0.0.exe في release/
```

> قاعدة البيانات بتقنية WASM خالصة (بدون أي وحدات أصلية)، لذا لا حاجة لأدوات بناء
> ولا لخطوات إضافية — `npm install` ثم `npm run dist` مباشرة على Windows.

المثبت: NSIS مع اختيار مجلد التثبيت + اختصارات سطح المكتب وقائمة ابدأ + إلغاء تثبيت.
البيانات تُحفظ في `%APPDATA%/Smart Secretary/data` وتبقى بعد التحديث/إعادة التثبيت.

## البنية

```
electron/      غلاف سطح المكتب (main + preload)
server/        API + قاعدة البيانات + التقارير (db/auth/api/api2/index)
client/src/    الواجهة (lib + components + pages)
docs/          التحليل والمعمارية والتقرير النهائي
scripts/test.js جناح الاختبارات (46 فحصًا)
data/          قاعدة البيانات والملفات والنسخ (تُنشأ تلقائيًا)
```

التوثيق الكامل: `docs/ANALYSIS.md` • `docs/ARCHITECTURE.md` • `docs/FINAL-REPORT.md`
